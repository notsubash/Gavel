/** Core worksheet fields that create a new version when changed (matches backend). */
export const CORE_WORKSHEET_FIELDS = new Set([
  "audience",
  "problem_statement",
  "solution_statement",
  "secret_sauce",
  "pricing_hypothesis",
  "top_risky_assumption",
]);
